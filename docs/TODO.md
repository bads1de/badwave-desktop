# TODO - BadWave Windows

## 🚀 パフォーマンス改善

### プリフェッチ機能の追加

**優先度**: 中
**ステータス**: 未着手

現在、SWR パターンは正しく動作しているが、**初回アクセス時**はキャッシュが空のためローディングが発生する。

#### 現状

- 初回アクセス → サーバーフェッチ → ローディング表示
- 2 回目以降 → キャッシュから即座に表示 ✅
- アプリ再起動後 → 永続化キャッシュから復元 ✅

#### 改善案

1. **アプリ起動時に主要データをプリフェッチ**

   - LikedSongs
   - Playlists
   - Recommendations
   - シンプルだが、起動が少し遅くなる可能性

2. **サイドバーホバー時にプリフェッチ** (Spotify 風)
   - ユーザーの意図を予測して先読み
   - より洗練された UX

#### 実装例

```tsx
// 例：ホーム画面でLikedSongsをプリフェッチ
import { useQueryClient } from "@tanstack/react-query";

const queryClient = useQueryClient();

// マウント時にプリフェッチ
useEffect(() => {
  if (userId) {
    queryClient.prefetchQuery({
      queryKey: ["likedSongs", userId],
      queryFn: () => fetchLikedSongs(userId),
    });
  }
}, [userId]);
```

#### 参考

- [TanStack Query - Prefetching](https://tanstack.com/query/latest/docs/framework/react/guides/prefetching)

---

## 📝 その他の TODO

<!-- 今後のTODOをここに追加 -->
